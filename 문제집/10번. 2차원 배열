#include <iostream>
#include <vector>

using namespace std;

int main() 
{
  int grid[4][4] = { {} };
	int sum = 0;

	for (int i = 0; i < 4; i++)
	{
		for (int j = 0; j < 4; j++)
		{
			cin >> grid[i][j];

			if (i >= j)
				sum += grid[i][j];
		}
	}
 
	cout << sum;

  return 0;
}
